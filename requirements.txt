# Provider extras installed so LLM_PROVIDER can switch at runtime.
# anthropic    → Claude models (ANTHROPIC_API_KEY)
# google-genai → Gemini models (GEMINI_API_KEY)
# openai       → OpenAI models (OPENAI_API_KEY)
crewai[anthropic,google-genai,openai]>=0.80.0
faker>=24.0.0
duckdb>=0.10.0
pandas>=2.0.0          # required by duckdb .fetchdf() in the profiler/simulator tools
pyyaml>=6.0.1
pydantic>=2.0.0
python-dotenv>=1.0.0
datacontract-cli>=0.10.0
streamlit>=1.40.0      # web UI (app.py) — run with: streamlit run app.py
